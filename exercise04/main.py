from the_solutions import square_it, change_list, lambda_version
from unittest import main

the_list = [3, 2, 1]
print(change_list(the_list, square_it))


# Runs tests automatically
main(module='test_exercise_four', exit=False)
